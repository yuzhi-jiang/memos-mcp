[project]
name = "memos-cmp"
version = "0.1.0"
description = "Memos MCP服务器"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.6.0",
    "requests>=2.32.3",
]
